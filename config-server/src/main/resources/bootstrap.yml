spring.application.name: config-server
#encrypt.key: ${MY_CONFIG_ENCRYPT_KEY:my-secret-encryption-key}

encrypt:
  key-store:
    location: classpath:server.jks
    password: letmein
    alias: mytestkey
    secret: changeme